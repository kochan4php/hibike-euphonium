import Layout from "../layout";

const Home = () => (
  <Layout>
    <section
      className="min-h-screen bg-cover bg-center"
      style={{ backgroundImage: "url('https://source.unsplash.com/1200x500')" }}
    >
      <div className="container mx-auto text-white min-h-screen">
        {/* <h1 className="text-5xl text-center">Tes Doang</h1> */}
      </div>
    </section>
  </Layout>
);

export default Home;
